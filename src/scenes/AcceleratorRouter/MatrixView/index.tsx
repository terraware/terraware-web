import React from 'react';

import Page from 'src/components/Page';
import strings from 'src/strings';

const MatrixView = () => {
  return <Page title={strings.MATRIX_VIEW} />;
};

export default MatrixView;
