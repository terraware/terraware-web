import React from 'react';

import strings from 'src/strings';

import ApplicationPage from '../ApplicationPage';

const PrescreenView = () => {
  return <ApplicationPage title={strings.PRESCREEN}></ApplicationPage>;
};

export default PrescreenView;
