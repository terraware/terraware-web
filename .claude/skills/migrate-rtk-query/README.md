```aiignore
Example prompts:
- Replace the ProjectsService.getProject usages (requestProject thunk) with rtk query. The query should already exist
- Use the @.claude/skills/migrate-rtk-query/SKILL.md to replace the listProjects endpoint with rtk query
```
